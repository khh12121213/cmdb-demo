# 银行 CI/CD-CMDB 系统 — 接口文档

> 版本：v0.1.0 | Base URL：`http://{host}:{port}/api/v1`

---

## 约定

- 所有接口返回 JSON，成功时 `{"success":true, ...}`
- 错误时 HTTP 状态码 4xx/5xx + `{"detail":"错误描述"}`
- 分页统一：`page`(1起始), `size`(条数)，返回 `{"items":[], "total":N}`
- 逻辑删除：DELETE/PUT remove 仅设置 `is_deleted=1`，不做物理删除
- 时间格式：ISO-8601 / MySQL DATETIME

---

## 1. 服务健康

### GET /api/health
> 基础存活检查

响应：
```json
{"status": "ok"}
```

### GET /api/ready
> 存活 + 数据库连通性检查

响应：
```json
{"status": "ok", "db": "connected"}
```

---

## 2. Jenkins CD 流水线接口（核心）

### 2.1 POST /api/v1/cd/release-before — 发布前置
> 告警管控 + 发布锁抢占。幂等：同一 trace_id 重复调用直接返回成功，不重复执行。

请求：
```json
{
  "release_trace_id": "UUID-20260619-001",
  "env_code": "prod",
  "app_code": "loan-core",
  "group_code": "default-group",
  "release_no": "J-20260619.1",
  "timeout_minute": 60
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| release_trace_id | string | ✅ | 全局发布追踪 ID（UUID），幂等键 |
| env_code | string | ✅ | 环境编码 |
| app_code | string | ✅ | 应用编码 |
| group_code | string | ❌ | 分组编码，默认 default-group |
| release_no | string | ✅ | Jenkins 构建编号 |
| timeout_minute | int | ❌ | 锁超时分钟，默认 60 |

成功响应 (200)：
```json
{"success": true, "message": "发布锁抢占成功"}
```

幂等响应 (200)：
```json
{"success": true, "message": "幂等：工单已存在", "idempotent": true}
```

冲突响应 (409)：
```json
{"detail": "部署组已被锁定，当前发布中"}
```

### 2.2 POST /api/v1/cd/get-deploy-target — 获取部署配置
> 返回全量部署配置，自动过滤维护主机（instance_status=0）

请求：
```json
{
  "env_code": "prod",
  "app_code": "loan-core",
  "cluster_code": "",
  "group_code": "default-group"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| env_code | string | ✅ | 环境编码 |
| app_code | string | ✅ | 应用编码 |
| cluster_code | string | ❌ | 集群编码，空则查所有 |
| group_code | string | ❌ | 分组编码，默认 default-group |

成功响应 (200)：
```json
{
  "success": true,
  "data": {
    "group": {
      "artifact_file_name": "biz.tar",
      "deploy_path": "/data/app/loan-core",
      "deploy_user": "appdeploy",
      "deploy_strategy": "full",
      "unpack_flag": "Y",
      "health_check_url": "http://127.0.0.1:8080/health",
      "jvm_opts": "-Xms2g -Xmx4g",
      "start_script": "./start.sh",
      "stop_script": "./stop.sh"
    },
    "instances": [
      {
        "id": 1001,
        "instance_ip": "10.20.30.1",
        "bk_biz_id": 200,
        "bk_host_id": 5001,
        "bk_cloud_id": 0,
        "bk_module_id": 300,
        "bk_inner_ip": "10.20.30.1",
        "instance_tags": "group-a"
      }
    ],
    "variables": {
      "DB_HOST": "10.20.30.100",
      "REDIS_HOST": "10.20.30.200"
    }
  }
}
```

**变量合并优先级**：应用全局 < 集群 < 部署组 < 单机实例。敏感变量（is_secret=1）自动 AES 解密返回。

### 2.3 POST /api/v1/cd/release-after — 发布后置
> 版本回写 + 告警恢复 + 锁释放。幂等。

请求：
```json
{
  "release_trace_id": "UUID-20260619-001",
  "env_code": "prod",
  "app_code": "loan-core",
  "cluster_id": 10,
  "group_code": "default-group",
  "build_no": "J-20260619.1",
  "artifact_version": "账务补丁20260619",
  "release_user": "release-op",
  "overall_status": "partial_success",
  "instances": [
    {"instance_id": 1001, "instance_ip": "10.20.30.1", "deploy_result": "success"},
    {"instance_id": 1002, "instance_ip": "10.20.30.2", "deploy_result": "fail"}
  ]
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| release_trace_id | string | ✅ | 全局发布追踪 ID，幂等键 |
| env_code | string | ✅ | 环境编码 |
| app_code | string | ✅ | 应用编码 |
| cluster_id | int | ✅ | 集群 ID |
| group_code | string | ❌ | 分组编码 |
| build_no | string | ✅ | Jenkins 构建编号 |
| artifact_version | string | ✅ | 投产版本备注 |
| release_user | string | ❌ | 发布人，默认 jenkins-cd |
| overall_status | string | ❌ | success / partial_success / fail |
| instances | array | ❌ | 单机部署结果 |

**基线更新规则**：
- `overall_status = success / partial_success` → 事务清空旧基线 is_current，新记录生效
- `overall_status = fail` → 不切换基线，只写快照留痕

成功响应 (200)：
```json
{"success": true, "message": "发布完成"}
```

### 2.4 GET /api/v1/cd/release-status — 锁状态查询

请求：`GET /api/v1/cd/release-status?env_code=prod&app_code=loan-core&group_code=default-group`

响应：
```json
{"locked": true, "trace_id": "UUID-20260619-001", "ttl_seconds": 3250}
```

---

## 3. 运维管理接口

### 3.1 环境管理

#### GET /api/v1/admin/env/list
> 分页查询环境

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| page | int | 1 | 页码 |
| size | int | 20 | 每页条数 |
| tags | string | "" | 权限标签过滤 |

响应：
```json
{"items": [{"id":1,"env_code":"prod","env_name":"生产环境","env_tags":"ops-team"}], "total":1}
```

#### POST /api/v1/admin/env/save
> 新增/编辑环境（有 id 则更新，无 id 则新增）

请求：
```json
{"env_code": "prod", "env_name": "生产环境", "env_tags": "ops-team"}
```

#### PUT /api/v1/admin/env/remove
> 逻辑删除

请求：`?id=1`

---

### 3.2 应用管理

#### GET /api/v1/admin/app/list
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| page | int | 1 | |
| size | int | 20 | |
| keyword | string | "" | 按 app_code/app_name 模糊搜索 |

#### GET /api/v1/admin/app/all
> 获取全部应用（下拉选择用）

响应：
```json
[{"id":1,"app_code":"loan-core","app_name":"贷款核心系统","app_type":"springboot","owner":"ops-team"}]
```

#### GET /api/v1/admin/app/detail
> 应用详情

请求：`?id=1`

#### POST /api/v1/admin/app/save

请求：
```json
{
  "app_code": "loan-core",
  "app_name": "贷款核心系统",
  "app_type": "springboot",
  "server_port": 8080,
  "owner": "ops-team",
  "repo_url": "",
  "artifact_repo": "",
  "management_port": 8081,
  "proc_name": "loan-core-server",
  "base_jvm_opts": "-Xms2g -Xmx4g",
  "log_base_dir": "/data/logs/loan-core",
  "default_bk_biz_id": 200
}
```

enum `app_type`：`springboot | nginx | tongweb | weblogic | tsf-service`

#### PUT /api/v1/admin/app/remove
> 逻辑删除。请求：`?id=1`

---

### 3.3 集群管理

#### GET /api/v1/admin/cluster/list
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| env_code | string | "" | 环境过滤 |
| app_code | string | "" | 应用过滤 |
| page | int | 1 | |
| size | int | 20 | |

#### POST /api/v1/admin/cluster/save

请求：
```json
{
  "env_code": "prod",
  "app_code": "loan-core",
  "cluster_code": "prod-c1",
  "cluster_name": "生产集群1",
  "deploy_mode": "fixed",
  "tsf_cluster_id": "",
  "namespace": "",
  "labels": "",
  "status": 1
}
```

| 字段 | 说明 |
|------|------|
| deploy_mode | `fixed` = 固定虚拟机，`elastic` = 弹性容器 |
| status | 1 = 允许发布，0 = 禁用投产 |

#### PUT /api/v1/admin/cluster/status
> 快速切换集群开关

请求：`?id=1&status=0`

#### PUT /api/v1/admin/cluster/remove
> 逻辑删除。请求：`?id=1`

---

### 3.4 部署组管理

#### GET /api/v1/admin/group/list
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| env_code | string | "" | |
| app_code | string | "" | |
| page | int | 1 | |
| size | int | 20 | |

#### POST /api/v1/admin/group/save

请求：
```json
{
  "env_code": "prod",
  "app_code": "loan-core",
  "cluster_id": 1,
  "group_code": "default-group",
  "group_name": "默认分组",
  "group_type": "fixed",
  "status": 1,
  "artifact_file_name": "biz.tar",
  "deploy_path": "/data/app/loan-core",
  "deploy_user": "appdeploy",
  "deploy_strategy": "full",
  "unpack_flag": "Y",
  "jvm_opts": "-Xms2g -Xmx4g",
  "health_check_url": "http://127.0.0.1:8080/health",
  "start_script": "./start.sh",
  "stop_script": "./stop.sh"
}
```

| 核心字段 | 说明 |
|------|------|
| group_type | `fixed` 固定主机 / `elastic` 弹性容器 |
| artifact_file_name | 服务器上固定包名，不带版本 |
| deploy_strategy | `full` 全量 / `incr` 增量 |
| unpack_flag | `Y` 解压部署 / `N` 不解压 |
| lock_status | 发布锁状态（只读），0=空闲 1=发布中 |

**约束**：fixed 虚拟机集群仅允许 1 条 default-group，不可创建多条。

#### PUT /api/v1/admin/group/remove
> 逻辑删除。请求：`?id=1`

---

### 3.5 主机实例管理

#### GET /api/v1/admin/instance/list
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| group_id | int | 0 | 按部署组筛选，0=全部 |
| page | int | 1 | |
| size | int | 50 | |

#### POST /api/v1/admin/instance/save

请求：
```json
{
  "deploy_group_id": 1,
  "instance_ip": "10.20.30.1",
  "ssh_port": 22,
  "bk_biz_id": 200,
  "bk_host_id": 5001,
  "bk_cloud_id": 0,
  "bk_module_id": 300,
  "bk_inner_ip": "10.20.30.1",
  "instance_tags": "group-a",
  "instance_status": 1
}
```

| 字段 | 说明 |
|------|------|
| bk_host_id | 蓝鲸主机唯一 ID（作业平台主键） |
| bk_biz_id | 蓝鲸业务 ID |
| bk_cloud_id | 蓝鲸云区域 ID |
| instance_status | 1 正常可用 / 0 维护禁用 |
| instance_tags | 分批标签，如 group-a / group-b |

#### PUT /api/v1/admin/instance/status
> 快速切换主机维护/正常

请求：`?id=1&status=0`

#### PUT /api/v1/admin/instance/bind-group
> 批量绑定主机到部署组

请求 Body：
```json
{"ids": [1, 2, 3], "group_id": 5}
```

响应：
```json
{"success": true, "count": 3}
```

---

### 3.6 变量管理

#### GET /api/v1/admin/variable/list
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| env_code | string | "" | |
| app_code | string | "" | |
| page | int | 1 | |
| size | int | 50 | |
| show_secret | bool | false | 是否显示加密值原文 |

#### POST /api/v1/admin/variable/save

请求：
```json
{
  "env_code": "prod",
  "app_code": "loan-core",
  "cluster_code": "",
  "group_code": "default-group",
  "instance_id": 0,
  "var_key": "DB_HOST",
  "var_value": "10.20.30.100",
  "is_secret": 0,
  "remark": "数据库地址"
}
```

| 字段 | 说明 |
|------|------|
| is_secret | 1 = 敏感值，AES 加密存储 |
| instance_id | 0 表示不限主机，>0 绑定指定主机 |
| 优先级 | cluster_code/group_code/instance_id 越精确优先级越高 |

#### DELETE /api/v1/admin/variable/remove
> 逻辑删除。请求：`?id=1`

---

### 3.7 发布基线（只读）

#### GET /api/v1/admin/release/baseline
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| env_code | string | "" | |
| app_code | string | "" | |
| page | int | 1 | |
| size | int | 20 | |

#### GET /api/v1/admin/release/instance-snaps
> 按实例 ID 查询单机发布快照

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| instance_id | int | 0 | 0 = 全部 |
| page | int | 1 | |
| size | int | 20 | |

---

### 3.8 审计日志

#### GET /api/v1/admin/audit/list
| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| page | int | 1 | |
| size | int | 20 | |
| operator | string | "" | 操作人过滤 |
| operation | string | "" | 操作类型过滤 |

---

### 3.9 枚举字典

#### GET /api/v1/admin/enums

响应：
```json
{
  "app_types": ["springboot","nginx","tongweb","weblogic","tsf-service"],
  "deploy_modes": ["fixed","elastic"],
  "deploy_strategys": ["full","incr"],
  "package_types": ["jar","war","tar"],
  "env_codes": ["dev","test","staging","prod"]
}
```

---

## 4. 错误码对照

| HTTP 状态码 | 场景 |
|-------------|------|
| 200 | 成功 |
| 400 | 参数校验失败 |
| 404 | 资源不存在（部署组未配置等） |
| 409 | 冲突（锁已占用、重复操作等） |
| 500 | 服务内部错误 |

---

## 5. 鉴权约定（Demo 阶段未启用）

上线前需补充：
- **管理端**：对接 SSO，`app_code` 行权限 + `env_tags` 环境隔离
- **流水线**：Token 绑定 `env_code` + `app_code`，禁止全局 Token
- **敏感操作**：加密变量明文查看二次授权 + 审计留痕
