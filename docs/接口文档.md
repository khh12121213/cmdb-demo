# 银行 CI/CD-CMDB 系统 — 接口文档

> 版本：v0.1.0 | Base URL：`http://{host}:8000`

---

## 约定

- 所有接口返回 JSON，成功时 `{"success":true, ...}` 或 `{"items":[], "total":N}`
- 错误时 HTTP 4xx/5xx + `{"detail":"错误描述"}`
- 分页统一：`page` (1 起始), `size` (每页条数)，返回 `{"items":[], "total":N}`
- 逻辑删除：PUT/DELETE remove 仅设置 `is_deleted=1`，不做物理删除
- 时间格式：ISO-8601 / MySQL DATETIME

---

## 1. 服务健康

### `GET /api/health`

基础存活检查。

响应：`{"status": "ok"}`

### `GET /api/ready`

存活 + 数据库连通性检查。

响应：`{"status": "ok", "db": "connected"}`

---

## 2. Jenkins CD 流水线 API（`/api/v1/cd`）

### 2.1 `POST /api/v1/cd/release-before`

> 发布前置：告警管控 + 发布锁抢占

**请求体：**

| 字段 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|
| release_trace_id | string | ✅ | — | 全局发布追踪 ID（UUID），幂等键 |
| env_code | string | ✅ | — | 环境编码 |
| app_code | string | ✅ | — | 应用编码 |
| group_code | string | ❌ | `"default-group"` | 部署分组编码 |
| release_no | string | ✅ | — | Jenkins 构建编号 |
| timeout_minute | int | ❌ | 60 | 锁超时分钟数 |

**响应：**

| 场景 | HTTP 状态码 | body |
|------|------------|------|
| 锁抢占成功 | 200 | `{"success":true,"message":"发布锁抢占成功"}` |
| 幂等（trace已存在） | 200 | `{"success":true,"message":"幂等：工单已存在","idempotent":true}` |
| 锁已被其他工单占用 | 409 | `{"detail":"部署组已被锁定，当前发布中"}` |

**功能时序：**
1. 检查 `sys_audit_log.trace_id` 是否已存在 → 幂等返回
2. DB 乐观锁：`UPDATE app_deploy_group SET lock_status=1 WHERE id=xxx AND lock_status=0`
3. Redis 锁：`SET lock:deploy:{env}:{app}:{group} {trace_id} NX EX {timeout_minute*60}`
4. 写入审计日志 `PUBLISH_BEFORE`

---

### 2.2 `POST /api/v1/cd/get-deploy-target`

> 获取全量发布配置

**请求体：**

| 字段 | 类型 | 必填 | 默认 | 说明 |
|------|------|------|------|------|
| env_code | string | ✅ | — | 环境编码 |
| app_code | string | ✅ | — | 应用编码 |
| cluster_code | string | ❌ | `""` | 集群编码，空则查全部 |
| group_code | string | ❌ | `"default-group"` | 部署分组编码 |

**响应（fixed 模式 — 固定主机）：**

```json
{
  "success": true,
  "data": {
    "env_code": "prod",
    "app_code": "loan-core",
    "group_type": "fixed",
    "package_type": "tar",
    "artifact_file_name": "biz.tar",
    "artifact_id": "cvm-pkg-001",
    "deploy_path": "/data/app/loan-core",
    "deploy_user": "appdeploy",
    "deploy_strategy": "full",
    "unpack_flag": "Y",
    "jvm_opts": "-Xms2g -Xmx4g",
    "health_check_url": "http://127.0.0.1:8080/health",
    "start_script": "./start.sh",
    "stop_script": "./stop.sh",
    "instances": [
      {
        "instance_id": 1,
        "instance_ip": "10.20.30.1",
        "ssh_port": 22,
        "instance_status": 1,
        "bk_biz_id": 200,
        "bk_host_id": 5001,
        "bk_cloud_id": 0,
        "bk_module_id": 300,
        "bk_inner_ip": "10.20.30.1",
        "instance_tags": "group-a",
        "deploy_user": "",
        "deploy_path": "",
        "health_check_url": "",
        "start_script": "",
        "stop_script": ""
      }
    ],
    "variables": {
      "DB_HOST": "10.20.30.100",
      "REDIS_HOST": "10.20.30.200"
    }
  }
}
```

**响应（elastic 模式 — TSF 弹/性容器）：**

```json
{
  "success": true,
  "data": {
    "env_code": "prod",
    "app_code": "loan-core",
    "group_type": "elastic",
    "container_platform": "tsf",
    "image_repo": "registry.bank.com/library",
    "image_name": "loan-core",
    "deploy_group_id": "tsf-deploy-group-001",
    "tsf_config": {
      "deploy_group_id": "tsf-deploy-group-001",
      "cpu_request": "500m",
      "cpu_limit": "2000m",
      "mem_request": "512Mi",
      "mem_limit": "2048Mi",
      "replicas": 3,
      "tsf_traffic_weight": 0,
      "update_type": 0
    },
    "variables": {
      "DB_HOST": "10.20.30.100"
    }
  }
}
```

| data 字段 | 说明 |
|----------|------|
| group_type | `fixed`（虚拟机）/ `elastic`（容器） |
| container_platform | elastic 时有效：`tsf` / `tke` / `docker` |
| artifact_id | TSF CVM 虚拟机软件包唯一 ID（fixed 模式） |
| image_repo / image_name | 容器镜像信息（elastic 模式） |
| instances | 仅 fixed 模式，已过滤 `instance_status=0` 维护态主机 |
| tsf_config | 仅 elastic 模式，内容见上表 |
| variables | 多级变量合并（env < app < cluster < group < instance），同 key 高优先级覆盖低优先级 |

---

### 2.3 `POST /api/v1/cd/release-after`

> 发布后置：版本回写 + 告警恢复 + 锁释放

**请求体：**

```json
{
  "release_trace_id": "UUID-20260619-001",
  "env_code": "prod",
  "app_code": "loan-core",
  "cluster_id": 10,
  "group_code": "default-group",
  "build_no": "J-20260619.1",
  "artifact_version": "账务补丁20260619",
  "release_user": "release-op",
  "overall_status": "partial_success",
  "instances": [
    {"instance_id": 1, "instance_ip": "10.20.30.1", "deploy_result": "success"},
    {"instance_id": 2, "instance_ip": "10.20.30.2", "deploy_result": "fail"}
  ]
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| release_trace_id | string | ✅ | 与 release-before 传入值一致，幂等键 |
| env_code | string | ✅ | 环境编码 |
| app_code | string | ✅ | 应用编码 |
| cluster_id | int | ✅ | 集群 ID |
| group_code | string | ✅ | 分组编码 |
| build_no | string | ✅ | Jenkins 构建号 |
| artifact_version | string | ✅ | 版本描述（用户可读） |
| release_user | string | ✅ | 操作人 |
| overall_status | string | ✅ | `success` / `fail` / `partial_success` |
| instances | array | ❌ | 单机部署结果列表 |

**响应：** `{"success": true, "message": "发布后置完成"}`

**功能时序：**
1. 幂等检查 `sys_audit_log.trace_id` + `PUBLISH_AFTER`
2. 写入 `app_deploy_group_release`（基线，`version` 乐观锁）
3. 逐条写入 `cluster_instance_release`（单机快照）
4. 释放 DB + Redis 锁（Lua 脚本校验 trace_id 后 DEL）
5. 写入审计日志 `PUBLISH_AFTER`

---

### 2.4 `GET /api/v1/cd/release-status`

> 查询发布锁状态（Redis）

| 参数 | 类型 | 必填 | 默认 |
|------|------|------|------|
| env_code | string | ✅ | — |
| app_code | string | ✅ | — |
| group_code | string | ❌ | `"default-group"` |

**响应：**

```json
{
  "locked": true,
  "trace_id": "UUID-20260619-001",
  "ttl_seconds": 3590
}
```

| 字段 | 说明 |
|------|------|
| locked | Redis key 是否存在 |
| trace_id | 锁发布时的 trace_id（Redis value） |
| ttl_seconds | 剩余过期时间（秒），`-1` 表示不存在 |

---

## 3. 运维管理 API（`/api/v1/admin`）

### 3.1 环境管理

#### `GET /api/v1/admin/env/list`

| 参数 | 类型 | 必填 | 默认 |
|------|------|------|------|
| page | int | ❌ | 1 |
| size | int | ❌ | 20 |
| tags | string | ❌ | `""` |
| keyword | string | ❌ | `""` |

> `tags` 是 env_tags 模糊匹配；`keyword` 匹配 env_code / env_name

#### `GET /api/v1/admin/env/all`

无参数，返回全量环境列表（供前端级联下拉使用）

#### `POST /api/v1/admin/env/save`

```json
{"env_code": "prod", "env_name": "生产环境", "env_tags": "ops-team"}
```

- 更新时传入 `id` 字段；新增省略 `id`

#### `PUT /api/v1/admin/env/remove`

参数：`?id=1`

---

### 3.2 系统管理

> 系统跨环境独立，不绑定环境。同一系统可有 dev/test/staging/prod 多环境下的应用。

#### `GET /api/v1/admin/sys/list`

| 参数 | 类型 | 默认 |
|------|------|------|
| page | int | 1 |
| size | int | 20 |

#### `POST /api/v1/admin/sys/save`

```json
{
  "sys_code": "loan-sys",
  "sys_name": "贷款业务系统",
  "dev_owner": "dev-team",
  "ops_owner": "ops-team",
  "remark": "核心业务系统"
}
```

#### `PUT /api/v1/admin/sys/remove`

参数：`?id=1`

---

### 3.3 应用管理

#### `GET /api/v1/admin/app/list`

| 参数 | 类型 | 默认 |
|------|------|------|
| page | int | 1 |
| size | int | 20 |
| keyword | string | `""` |

> keyword 匹配 app_code / app_name。返回含 `sys_name` 外联字段。

#### `GET /api/v1/admin/app/all`

无参数，返回全部应用（前端级联下拉用）

#### `GET /api/v1/admin/app/detail`

参数：`?id=1`

#### `POST /api/v1/admin/app/save`

```json
{
  "sys_id": 1,
  "app_code": "loan-core",
  "app_name": "贷款核心系统",
  "app_type": "springboot",
  "server_port": 8080,
  "management_port": 8081,
  "owner": "ops-team",
  "dev_owner": "dev-team",
  "ops_owner": "ops-team",
  "repo_url": "",
  "artifact_repo": "",
  "proc_name": "loan-core-server",
  "base_jvm_opts": "-Xms2g -Xmx4g",
  "log_base_dir": "/data/logs/loan-core",
  "default_bk_biz_id": 200
}
```

| 字段 | 说明 |
|------|------|
| sys_id | 归属系统 ID（关联 sys_info） |
| app_type | springboot / nginx / tongweb / weblogic / tsf-service / docker / bes / websphere / tomcat / mysql / redis / kafka / es / mongodb / zookeeper / other |

#### `PUT /api/v1/admin/app/remove`

参数：`?id=1`

---

### 3.4 集群管理

#### `GET /api/v1/admin/cluster/list`

| 参数 | 类型 | 默认 |
|------|------|------|
| env_code | string | `""` |
| app_code | string | `""` |
| page | int | 1 |
| size | int | 20 |

#### `POST /api/v1/admin/cluster/save`

```json
{
  "env_code": "prod",
  "app_code": "loan-core",
  "cluster_code": "prod-c1",
  "cluster_name": "生产集群1",
  "deploy_mode": "fixed",
  "container_platform": "",
  "tsf_cluster_id": "",
  "namespace": "",
  "labels": "",
  "status": 1
}
```

| 新增字段 | 说明 |
|---------|------|
| container_platform | elastic 模式生效：`tsf` / `tke` / `docker`；fixed 模式留空 |

- deploy_mode: `fixed`（固定主机）/ `elastic`（弹性容器）
- elastic 模式且 container_platform=tsf 时 tsf_cluster_id 和 namespace 有意义

#### `PUT /api/v1/admin/cluster/status`

参数：`?id=1&status=1`（status: 1 允许发布 / 0 禁用投产）

#### `PUT /api/v1/admin/cluster/remove`

参数：`?id=1`

---

### 3.5 部署组管理

#### `GET /api/v1/admin/group/list`

| 参数 | 类型 | 默认 |
|------|------|------|
| env_code | string | `""` |
| app_code | string | `""` |
| page | int | 1 |
| size | int | 20 |

#### `POST /api/v1/admin/group/save`

**虚拟机部署组（group_type=fixed）：**

```json
{
  "env_code": "prod", "app_code": "loan-core", "cluster_id": 1,
  "group_code": "default-group", "group_name": "默认分组",
  "group_type": "fixed", "status": 1,
  "deploy_group_id": "", "package_type": "tar",
  "artifact_file_name": "biz.tar", "artifact_id": "cvm-pkg-001",
  "deploy_path": "/data/app/loan-core",
  "deploy_user": "appdeploy", "deploy_strategy": "full",
  "unpack_flag": "Y",
  "jvm_opts": "-Xms2g -Xmx4g",
  "health_check_url": "http://127.0.0.1:8080/health",
  "start_script": "./start.sh", "stop_script": "./stop.sh",
  "image_repo": "", "image_name": ""
}
```

**弹性容器部署组（group_type=elastic）：**

```json
{
  "env_code": "prod", "app_code": "loan-core", "cluster_id": 1,
  "group_code": "container-group", "group_name": "容器分组",
  "group_type": "elastic", "status": 1,
  "deploy_group_id": "tsf-deploy-group-001",
  "image_repo": "registry.bank.com/library",
  "image_name": "loan-core",
  "cpu_request": "500m", "cpu_limit": "2000m",
  "mem_request": "512Mi", "mem_limit": "2048Mi",
  "replicas": 3, "tsf_traffic_weight": 0, "update_type": 0,
  "artifact_file_name": "", "artifact_id": ""
}
```

| 新增字段 | 类型 | 说明 |
|---------|------|------|
| image_repo | string | 镜像仓库地址，TKE/Docker/TSF 容器共用 |
| image_name | string | 镜像名称 |
| artifact_id | string | TSF CVM 虚拟机软件包唯一 ID（fixed 模式） |

- **update_type**: 0=立即更新 / 1=滚动更新

#### `PUT /api/v1/admin/group/remove`

参数：`?id=1`

---

### 3.6 主机实例管理

#### `GET /api/v1/admin/instance/list`

| 参数 | 类型 | 默认 |
|------|------|------|
| group_id | int | 0（查全部） |
| page | int | 1 |
| size | int | 50 |

#### `POST /api/v1/admin/instance/save`

```json
{
  "deploy_group_id": 1, "instance_ip": "10.20.30.1",
  "ssh_port": 22, "instance_status": 1,
  "bk_biz_id": 200, "bk_host_id": 5001,
  "bk_cloud_id": 0, "bk_module_id": 300, "bk_inner_ip": "10.20.30.1",
  "instance_tags": "group-a",
  "deploy_user": "", "deploy_path": "",
  "health_check_url": "", "start_script": "", "stop_script": ""
}
```

#### `PUT /api/v1/admin/instance/status`

参数：`?id=1&status=1`（1=正常 / 0=维护禁用）

#### `PUT /api/v1/admin/instance/bind-group`

**批量绑定主机到部署组：**

参数：`?group_id=5`，请求体：`[1, 2, 3]`（主机 ID 列表）

---

### 3.7 变量管理

#### `GET /api/v1/admin/variable/list`

| 参数 | 类型 | 默认 |
|------|------|------|
| env_code | string | `""` |
| app_code | string | `""` |
| page | int | 1 |
| size | int | 50 |

#### `POST /api/v1/admin/variable/save`

```json
{
  "env_code": "prod", "app_code": "loan-core",
  "cluster_code": "", "group_code": "default-group",
  "instance_id": 0,
  "var_key": "DB_HOST",
  "var_value": "10.20.30.100",
  "remark": "数据库地址"
}
```

| 字段 | 说明 |
|------|------|
| cluster_code | 指定集群级作用域，留空不限定 |
| group_code | 指定部署组级作用域，留空不限定 |
| instance_id | 指定实例级作用域，0 为不限定 |

**变量合并优先级（高→低）：** `instance_id > group_code > cluster_code > app_code > env_code`

#### `DELETE /api/v1/admin/variable/remove`

参数：`?id=1`

---

### 3.8 发布基线 / 单机快照 / 审计（只读）

#### `GET /api/v1/admin/release/baseline`

| 参数 | 类型 | 默认 |
|------|------|------|
| env_code | string | `""` |
| app_code | string | `""` |
| page | int | 1 |
| size | int | 20 |

#### `GET /api/v1/admin/release/instance-snaps`

| 参数 | 类型 | 默认 |
|------|------|------|
| instance_id | int | 0 |
| page | int | 1 |
| size | int | 20 |

#### `GET /api/v1/admin/audit/list`

| 参数 | 类型 | 默认 |
|------|------|------|
| page | int | 1 |
| size | int | 20 |
| operator | string | `""` |
| operation | string | `""` |

> operation 枚举：INSERT/UPDATE/DELETE/PUBLISH/SYNC/SECRET_QUERY

---

### 3.9 枚举字典

#### `GET /api/v1/admin/enums`

返回前端下拉选项：

```json
{
  "app_types": ["springboot", "nginx", "tongweb", "weblogic", "tsf-service"],
  "deploy_modes": ["fixed", "elastic"],
  "deploy_strategys": ["full", "incr"],
  "package_types": ["jar", "war", "tar"],
  "env_codes": ["dev", "test", "staging", "prod"]
}
```

---

## 4. 静态文件托管

### `GET /{full_path}`

SPA 托管。当路径不存在时 fallback 到 `index.html`。
前端构建产物位于 `frontend/dist/`，由后端 `/assets/*` 和 `/` 路由统一托管。
快速访问：浏览器打开 `http://{host}:8000/` 即可进入管理界面。

---

## 5. 完整 API 速查表

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | /api/health | 存活检查 | ❌ |
| GET | /api/ready | 存活+DB连接 | ❌ |
| POST | /api/v1/cd/release-before | 发布前置+锁 | ❌ |
| POST | /api/v1/cd/get-deploy-target | 获取配置 | ❌ |
| POST | /api/v1/cd/release-after | 发布后置+释放 | ❌ |
| GET | /api/v1/cd/release-status | 锁状态 | ❌ |
| GET | /api/v1/admin/env/list | 环境列表 | ❌ |
| GET | /api/v1/admin/env/all | 全量环境 | ❌ |
| POST | /api/v1/admin/env/save | 新增/编辑环境 | ❌ |
| PUT | /api/v1/admin/env/remove | 逻辑删除环境 | ❌ |
| GET | /api/v1/admin/sys/list | 系统列表 | ❌ |
| POST | /api/v1/admin/sys/save | 新增/编辑系统 | ❌ |
| PUT | /api/v1/admin/sys/remove | 逻辑删除系统 | ❌ |
| GET | /api/v1/admin/app/list | 应用列表 | ❌ |
| GET | /api/v1/admin/app/all | 全量应用 | ❌ |
| GET | /api/v1/admin/app/detail | 应用详情 | ❌ |
| POST | /api/v1/admin/app/save | 新增/编辑应用 | ❌ |
| PUT | /api/v1/admin/app/remove | 逻辑删除应用 | ❌ |
| GET | /api/v1/admin/cluster/list | 集群列表 | ❌ |
| POST | /api/v1/admin/cluster/save | 新增/编辑集群 | ❌ |
| PUT | /api/v1/admin/cluster/status | 启用/禁用集群 | ❌ |
| PUT | /api/v1/admin/cluster/remove | 逻辑删除集群 | ❌ |
| GET | /api/v1/admin/group/list | 部署组列表 | ❌ |
| POST | /api/v1/admin/group/save | 新增/编辑部署组 | ❌ |
| PUT | /api/v1/admin/group/remove | 逻辑删除部署组 | ❌ |
| GET | /api/v1/admin/instance/list | 主机列表 | ❌ |
| POST | /api/v1/admin/instance/save | 新增/编辑主机 | ❌ |
| PUT | /api/v1/admin/instance/status | 正常/维护切换 | ❌ |
| PUT | /api/v1/admin/instance/bind-group | 批量绑定部署组 | ❌ |
| GET | /api/v1/admin/variable/list | 变量列表 | ❌ |
| POST | /api/v1/admin/variable/save | 新增/编辑变量 | ❌ |
| DELETE | /api/v1/admin/variable/remove | 逻辑删除变量 | ❌ |
| GET | /api/v1/admin/release/baseline | 发布基线列表 | ❌ |
| GET | /api/v1/admin/release/instance-snaps | 单机快照列表 | ❌ |
| GET | /api/v1/admin/audit/list | 审计日志列表 | ❌ |
| GET | /api/v1/admin/enums | 枚举字典 | ❌ |
| GET | /{full_path} | SPA 静态托管 | ❌ |

---

## 6. 错误码对照

| HTTP 状态码 | 场景 |
|-------------|------|
| 200 | 成功 |
| 400 | 参数校验失败 |
| 404 | 资源不存在 |
| 409 | 冲突（锁已占用、重复提交） |
| 422 | 请求体格式错误（FastAPI 内置校验） |
| 500 | 服务内部错误 |

---

## 7. 鉴权约定（Demo 阶段未启用）

上线前需补充：

- **管理端**：对接 SSO，`env_tags` 环境 + `app_code` 行级数据权限
- **流水线**：Token 鉴权，绑定 `env_code` + `app_code`，预生成验证
- **审计**：AOP 切面自动记录所有 CRUD 操作到 `sys_audit_log`（当前仅 `release_service` 手动写审计）
